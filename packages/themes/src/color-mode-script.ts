/**
 * Server-safe entry point for ColorModeScript.
 *
 * Import from '@stackwright/themes/color-mode-script' in Server Components
 * (e.g. App Router layout.tsx). This entry does NOT pull in ThemeProvider
 * or any other client-only code.
 */
export { ColorModeScript } from './ColorModeScript';
