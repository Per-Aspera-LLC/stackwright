---
"@stackwright/nextjs": patch
---

Raise minimum Next.js peer dependency from `>=15` to `>=15.2.3` to exclude versions with critical CVEs (authorization bypass in middleware, RCE in React flight protocol).
